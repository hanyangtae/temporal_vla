# Gate 2 — pq3 Phase A 코드 리뷰 (2026-07-15)

- **게이트**: Gate 2 (구현 코드 리뷰)
- **thread_id**: `019f641d-89bd-75f1-ae8d-cf2485067a96` (3 라운드, codex-cli 0.144.1, ask lane)
- **리뷰 범위**: `ea61d24..HEAD` (pq3 Phase A 코드 정렬 + Gate A 테스트)
- **운영 사고 기록**: `codex review` 전용 서브커맨드가 이 호스트의
  `use_legacy_landlock=true` 와 비호환(내부 landlock panic → 3시간 무산출 행)이라
  **ask lane(codex exec read-only)으로 대체 수행** — 스킬의 "전용 서브커맨드" 규칙 1회 예외.
  wrapper 에 0.144.x 제약(PROMPT 위치 인자·--base 와 동시 사용 불가) 가드 추가
  (`scripts/utils/codex_ask.sh` + 회귀 테스트).

## Round 1 (지적 19건: 치명 4 · 높음 13 · 중간 2) — 전건 코드 검증 후 수용

핵심 발견 (전부 실재 확인):

1. **[치명] 캡처-OFF 가 다른 추론 단위 사용** — groot `select_action` 은 16-큐 팝(16콜당
   1추론)이라 `--no-features`(/act)의 noise pairing·실행 단위가 캡처 경로와 상이.
   → `/act_with_features` `skip_features` 분기(hook 없는 predict_action_chunk) 신설,
   클라이언트 chunk 길이 assert, `/act`는 per-step steering 시 409.
2. **[치명] gated 부분 로드 무음 통과** → layer×phase Cartesian 완전성 abort,
   `--steering-phases` 기대 집합 일치, per-npz step 4개 검사.
3. **[치명] 포트의 기존 서버 오인(base 위약 우회)** → pre-kill + `[serve-boot] id` ↔
   /health `boot_id` 대조 + health steering 지문 + Address-in-use abort grep.
4. **[치명] ARM_SPEC 재라벨링 오염** → spec_sha 불변 + run_tag 사이드카 기록 +
   집계 본문 검증(중복·seed·run_tag).
5. 높음: per-step 미발화 검출(assert+6b 테스트) / fit 계약 심화(kind·axes·layers·K·T·D) /
   full 모드 dtype 광고 / legacy 기본값 복원(table14 는 명시) / Stage1 per-step 동일 객체
   quota / gated 성립 게이트 도구 신설(`pq3_gated_gate.py`) / freeze 원자화·fail-closed /
   plan-prefix·중복 assert / 전체 sha256·SHAS 필수 / dose 근거(phase_gated_flags +
   steer-norms) / β 경계·no-survivor / final_status·bootstrap CI 동결 / C0 cell registry 연결.
6. 중간: noise 시리즈 cell 오프셋(수집 500k+cell·eval 3M+cell 대역 분리) /
   host **cell-블록** 배정(한 cell 의 전 arm 동일 host — paired 상쇄).

반영 커밋: `7f320c8` (+ 유실 사건 공지 표준: 직송 size+sha ledger·평균 크기 상식 체크).

## Round 2 (잔여·신규 13건: 치명 3 · 높음 6 · 중간 4) — 전건 수용

배선 완결성 지적: gated phase 집합의 큐 전달 누락 / gated_gate enforce 부재(LOO 무효
통과) / NPZ sha 부분집합 검사 / β sweep SHA 미전달·no-survivor exit 0 / task-pooled
eval-reserved 단일 cell / worker rc·완주 검증 무음 / 집계 필드 부재 허용 / gated N/A
KeyError / dose 근거 휘발 / gated_gate 로더 계약 미검사 / Stage1 epeq 민감도 부재 /
succ 상충 스템 무음 선택 / collect-mode skip_features compile 오염.

반영 커밋: `946b57f`. Gate A(9항목) 재통과.

## 미채택 3건 (Round 2 에 제시, 재반론 없음 — 채택 종결)

1. per-call 적용 matrix key echo — 등록 정적 검증(완전성 abort + health 지문) +
   per-inference gated bool 로 갈음.
2. phase 를 /act payload 에 넣는 원자화 — uvicorn 단일 이벤트루프 직렬 실행 +
   포트당 단일 클라이언트 운영 규약으로 실런타임 경합 없음.
3. 구(action_token_mean) 모드 feature_dtype=float32 광고 유지 — legacy 소비자 계약.

## Round 3 (최종 확인 — 잔여 5건: 치명 1 · 높음 4, 전건 수용)

1. **[치명] gated 성립 게이트 ↔ 배포 NPZ 미결박** → gated_gate `--gated-npz-base` 신설
   (enforce 필수): LOO 를 배포 metadata 의 `selected_alpha_per_step` 으로 계산(대표 α 폐지),
   phase NPZ sha 집합을 report 에 기록, build_pq3_queue 가 arm-config sha 집합과 정확 대조.
2. **[높음] beta_sweep `${VAR:+X=v}` 셸 재분류 버그(rc=127 실증)** → env 배열로 교체.
3. **[높음] eval-reserved 필수·cell 집합 대조 미강제** → pq3 fit(require=all_token_full)은
   `--eval-reserved` 필수(exit 5), pooled manifest 의 pkl 경로 cell 집합 == reserved cell_id
   집합 정확 일치 강제 (+Gate A 회귀 테스트).
4. **[높음] gated N/A 수동 CLI 결정** → aggregate `--gated-gate-report` 필수, N/A 자동 파생,
   수동 override 제거.
5. **[높음] collect serve stale-server 경로** → eval 러너와 동일한 pre-kill·boot_id 대조·
   bind-error grep·EXIT trap 적용.

반영 커밋: (본 커밋). Gate A(9항목) 재통과. 왕복 상한(3회) 도달로 **Gate 2 종결** —
지적 수렴 추이 19→13→5건(전건 수용·구현), 실기 잔여 검증은 Phase B 스모크 S1–S9 가 수행.
