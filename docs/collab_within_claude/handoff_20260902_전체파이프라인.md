> **2026-09-02 후속(v5 첫 셀 게이트)**: 여기서 관찰된 "수집 실패 → 무개입 replay 성공 59%" 의 원인은
> replay 가 `EP_META_DIR`(ep_meta JSON 을 `reset(seed)` 전에 주입) 로 돌아 **k 번째 지터 상태가 수집과
> 달랐기 때문**이다(같은 좌표에서 drawer 방향까지 바뀜). JSON 미로드 replay 는 수집과 bit 동일. 상세
> `handoff_20260902_grid_recollect_v5.md` §0.5.1. 이 문서의 v4r 라벨은 수집 좌표와 다른 판의 라벨이다.

# Handoff — 중추(전체 파이프) 세션 (2026-09-02)

> [`handoff_20260831_perstep_cluster.md`](handoff_20260831_perstep_cluster.md) 대체. 구 문서는 8-31까지 기록용.
> **데이터는 전부 다시 모았음** → v4/v4sb/v4r 수치는 참고만. 
> 결과적으론 기존데이터와 머신이 같은 데이터는 같은 결과를 남김.
>이전과 일부 수집 머신이 바뀜

## 0. 이 세션의 역할

**중추 세션** = SAFE detector(언제) + phase 판정(무엇을) + steering 연산자(어떻게)를
per-step(매 step 판단, 개입) 파이프 하나로 묶어 end-to-end 효과 측정 → 병목을 파트 세션에 발주 → 개선분 재통합 → 다시 돌림.
직접 = 배선·eval 실행·집계·판정. 위임 = 파트 내부 설계.

**협업 세션** (ListAgents → SendMessage)
- `연산자 설계`: setM·LLR 채점기 fit, 추가로 다른 새로운 연산자를 만들 수 도 있음. 
- `Steering 고찰`: 설계 비판·위약/순환 논증
- `전체 파이프라인 dashboard`: 포크 관찰자 — {코드생성, process 실행} x
- `action phase`: phase detector 관리
- `시나리오 구체화`: 연구 주제의 중심내용 구체화
- `데이터 추가 수집`: 사용데이터의 종류, 저장형태, 모델, 위치 관리
- 원격 CPU = remote-compute 에이전트(승준). GPU eval = 중추가 직접 각 서버(48, 50, kanu)에 명령 실행.
- 서버 할당 규칙: §3 + `.claude/skills/robocasa-steer-eval/SKILL.md`

**대상**: GR00T N1.5 × RoboCasa. scene 5 × noise 5 × 지터 k 5 = 125판/instruction (v4 grid). 실패 = timeout(144 record).

instructions
1. open the left drawer
2. Fully slide the oven rack out
3. Fully slide the top dishwasher rack out
4. **Pick Place** Counter To Cabinet ~~5~~→ 4**종** (~~apple~~, bread, candle, jug, marshmallow) apple은 100% 성공
5. ~~setupcoffee~~ 실패만 너무 많이 모임
6. ~~open the right drawer~~ 성공만 너무 많이 모임

**용어**
- record = 모델 inference 1회(5 env-step마다). 발화 = detector가 그 record에서 fail 알림. 개입 = 발화 record의 action을 2차 pass로 교체(1회성).
- SAFE detector = DiT L12 activation(1536) 받는 LSTM. α = 허용 FPR(0.05/0.1/0.2, ckpt에 셋 다 있음). **β는 별개** = 연산자 개입 강도.
- phase = 개입 슬롯 키. GT = 이벤트 라벨러 phase. ck8 = activation AE(1536→16) 위 per-task KMeans k8(c0~c7).
- setM = 성공 setpoint 쪽으로 activation 당김(phase별 NPZ). condg = 조건부 대조 guidance(이번 미사용). reseed = denoise noise 재추첨 1회.
- rsN_llr = 발화 시 noise 후보 8개 재추첨(DiT-only, VLM 캐시) → LLR 채점기로 선택. llr = log_f − log_s, 낮을수록 성공스러움 → argmin. OOD 기각 = max(log_s, log_f) < ood_lo.
- fallback=reseed = setm이 미등록 phase에서 발화하면 reseed로 대체 개입.
- 구제 = 무개입 replay 실패 → 개입 arm 성공. 파손 = 반대(이번 측정 x).
- sidecar = 판별 json(`raw_rollouts/.../task0--ep*--succ*.json`). record열에 failure_scores / perstep_fired / gate_fallback / cand_* / rerun_ms.
- 지터 k = 같은 scene seed에서 배치·관절 k회 재추첨. cell_si = scene×100+k (s3_k1 → 301, kbase → 399).
- v4sb = 대상 scene 성공 배제 pool. v4r = 대상 scene 25판을 eval 파이프(무개입 replay + 캡처 ON)로 재수집한 것.
- rc=13 = per_episode 행 수 미달. fail-loud = 조용히 no-op 하지 말고 죽이기.
- 승준 = 원격 CPU 노드(kimseungjun@166.104.146.37:11112, shard·pkl 보관). kanu = 로컬 A4000×8. srv50/48 = A100(ssh `AISem_50_junhyeong`/`AISem_48_junhyeong`, worker2/worker1).
- "재시도" 두 뜻: hazard = 발화 지속 시 매 record 재추첨 / 운영 = 러너 job 재실행.

## 1. 이번에 한 것 (시간순)

### 1-0. 반전 문제
v4 수집 때 "실패"인 판을 eval 파이프에서 무개입 replay → **59%(36/61) 성공**. replay 자체는 완전 결정적(2회 불일치 0, 머신·GPU·캡처 바꿔도 bit 일치).
→ 구 수집 rollout만 다른 세계. 원인 조사 종료 → 데이터 재수집 했다. 문제해결.

### 1-1. rsN best-of-N 배선 (커밋 e202c30~c42ea61)
- serve `rsn_llr` op(`_run_resample_gate`), `src/failure_online/llr_scorer.py`(NPZ 계약 = docstring), 클라 `--perstep-n/--perstep-fallback`, 러너 `ps_rsn_llr` arm + `LLR_BUNDLE/LLR_SCENE/PERSTEP_N/PERSTEP_FALLBACK`.
- 로깅: 후보별 llr·entry·log_s/log_f·기각, 2차 pass 시간(rerun_ms·cand_ms). 발화 record 개입 비용 ≈ 50ms/후보.
- 고친 것: ① vla_client 매핑 누락 ② fallback을 gate_skipped에 넣어 applied 오집계 ③ **"현재 cluster" 기준 채점 = 전부 fallback** (발화는 후반 stall cluster, 등록은 초반 창) → 후보-기준 최근접 entry 채점(`score_nearest`), 발동 = SAFE 발화만 ④ 후보 전원 OOD → 연산자 쪽 ood_lo 상수항 버그 + 진성 격리 → 발화-분포로 재보정.
- arm 개편(사용자): ~~rsN_rand~~(≡reseed), setm GT/ck8 2종 추가, ~~β1.5~~ → β sweep 0.6~1.0.

### 1-2. 진단 3종 (Steering 고찰 제안, v4 정렬판 로그)
- 구제 셀 겹침: reseed 구제 5셀 ⊂ setm 구제 6셀. **구제 = 경계셀 속성**, 연산자 내용 x.
- hazard: 구제는 수십 회 재추첨 끝. 비구제는 100+회에도 0. → 1−(1−p)^K 산수 폐기. rsN_llr 판정은 ① 재추첨 횟수 누적곡선(reseed 대비) ② reseed가 못 구한 셀 전환(×3 재현 시만) ③ FP 파손 비회귀.
- triage: 구제셀 = 조기 발화(rec 22 vs 39)·비포화 score(.84 vs .96). oven(rec0 발화·.98)·jug(rec50 정발화) = 전판 비구제. 포화 score = 가망 없음 신호(연산 배분용).

### 1-3. v4sb → v4r (요약만. 데이터 전부 재수집 예정)
- v4sb 설계(포크): instruction별 balance scene 1개, fit pool = 타 4 scene + 대상 scene 실패만, setM은 scene 잔차화 후 instruction 단위, α 0.1, β 0.6~1.0.
- v4r: 대상 scene 25판 재수집(캡처 ON 무개입 replay). **eval도 같은 7-layer 캡처 hook 켜고 activation은 저장 x** (`DETECTOR_LAYERS=0,2,4,8,10,12,15`, 클라 `--no-features`).
- 175판 중 drawer-L 15판 제외 → 160판 / 실패 60. **drawer는 지터 k가 좌/우를 재추첨** (k2/kbase/k6 = right). left는 전승 → drawer-L eval 대상 없음.
- 재생성: detector_v4r(7 task, α 3종) · ae_v4r_k8 · instr_setm_v4r_gt(6) · instr_setm_v4r_ck8(5, oven 0) · rsn_llr_reg_v4r(18셀, oven 0). NPZ = plain `setpoint`.
- 가드: 첫 판 sidecar에서 setm 실적용 7/발화 16, llr 비후보0 선택 47/49 확인 후 완주.

### 1-4. v4r β sweep 결과 (참고용)
60판 × 12 arm, α 0.1, N8. 구제 = 재수집 실패 → 성공.

| arm | 구제 |
|---|---|
| reseed | 3/60 (oven 빼면 2/43) |
| rsn_llr | 5/43 (candle 3/8) |
| setm_gt β0.6 / 0.7 / 0.8 / 0.9 / 1.0 | 5/59 · 2/58 · 2/59 · 1/60 · 5/60 |
| setm_ck8 β0.6 / 0.7 / 0.8 / 0.9 / 1.0 | 3/43 · 3/42 · 1/43 · 2/43 · 5/43 |

- ck8·llr 분모 43 = 60 − oven 17(연산자 없음). 59/58/42 = kanu 러너 완강 실패 5 run(미조사).
- 구제율 최고 12%. 이전 17~21%는 라벨 반전 거품.
- jug(22)·oven(17) = 전 arm 0~2. 깊은 실패는 per-step으로 안 뒤집힘.
- rsn_llr > reseed (43판: 5 vs 2). 표본 작음.
- setm β 비단조. 판별력 부족.
- 파손 축 없음(전판 실패 판).

### 1-5. 혼합 세계 → 전부 재수집
fit pool 타 4 scene은 아직 구 수집 세계(detector 학습·CP·cluster·setM 방향). 대상 scene만 replay 세계.
→ **데이터 전부 다시 모으기로 결정.** 다음 라운드는 재수집 완료 후.

### 1-6. 구 표 주의
awk "0" 함정(§4-1)이 og_ck8v4·og_rsn 오케스트레이터에도 있었음 → 구 문서 §1-4 표도 noise-0 판 빠졌을 수 있음. 인용 시 각주.

## 2. 파트별 상태

| 파트 | 상태 | 다음 |
|---|---|---|
| detector | detector_v4r 7 task α3종 | 재수집 데이터로 재학습 |
| phase(cluster) | ae_v4r_k8 | 발화 위치≠등록 위치는 후보-기준 채점으로 우회 중. 실패 후반 창 기준 재등록은 채점기 개정 과제 |
| 연산자 | instr_setm_v4r_{gt,ck8}, rsn_llr_reg_v4r 18셀 | 재fit. oven ck8/llr 0 → setm·reseed만 |
| 수집 | v4r_collect 160판(승준 61G) | 전체 재수집 |
| 시나리오 | 재수집 정본 프레임 전달됨 | 파손 측정용 성공 판 arm 넣을지 |

## 3. 서버 규약 (갱신)
- **kanu**(A4000 16GB): serve ~5.8GB → **GPU당 serve 2**(6슬롯 = 3GPU×2). 3 GPU 상한·타인 프로세스 GPU 금지. serve는 lerobot 컨테이너. 4일 넘게 켜두면 NVML 죽음 → `docker restart lerobot`.
- **srv50 GPU1 / srv48 GPU2**(A100): host-conda serve(`~/miniconda3/envs/lerobot_050_groot/bin/python`, `SERVE_PYTHONPATH=~/pkt_ws/temporal_vla/lerobot/src`). **GPU당 6 병렬**. srv → 승준 직송 됨(`ssh -p 11112`, tar 스트림). repo `~/pkt_ws/temporal_vla` git pull, 자산은 tar.
- **승준**: `~/datasets/temporal_vla_store/groot/n15/analysis/grid_phase_v4/` 아래 v4r_collect(61G)·segA_v4r(24G)·segA_v4r_ck8(24G)·segA_v4sb·_ck8(50G, 보존만)·v4r_labels.tsv·instr_setm_v4r_*·rsn_llr_reg_v4r. 잔여 ~73G(700판이면 +55G). repo `~/workspace/temporal_vla`. 체인 스크립트 `~/workspace/temporal_vla/outputs/tmp/{v4sb,v4r}/run_*.sh`(git 밖, 로컬 outputs/tmp와 다른 파일).

## 4. 함정 (구 문서 §4에 추가)
1. **awk "0"**: `ns[key]=ns[key]?…` 가 noise "0"을 거짓 취급 → n0 판 무음 탈락(2회 재발). `(key in ns)` 로. **완주 후 매니페스트 대비 행수 감사 필수.**
2. 매니페스트 열: v4 index `armsig`는 전 행 'base'. 지터는 `jitter_reset_idx`(99=base). `cell_si`는 scene×100+k, per_episode `scene_idx`는 base scene. 섞지 말 것.
3. 캡처 수집 집계: grid 좌표로만 저장하면 raw_rollouts sidecar 없음 → INCOMPLETE → 러너 재시도가 자기 pkl과 덮어쓰기 tripwire 충돌. 이중기록 fix(7c1a11f). 같은 OUT_ROOT에 job 겹치면 같은 증상.
4. serve 즉사 = 30분 대기: 기동 실패(NPZ 포맷 등)여도 러너가 SERVE_BOOT_TRIES 다 기다림. `serve_<port>.log`에 `startup failed` 먼저 볼 것.
5. NPZ 포맷: instr-단위 fit = plain `setpoint`(alpha0_s/alpha0_v_steer) → 러너 `STEER_OP=setpoint`. seg판 = `setpoint_seg`.
6. drawer = 지터 k가 좌/우 재추첨. instruction 프레임 쓰려면 셀별 lang 확인.
7. pkill 자기매치·ssh setsid 붙들림·`local a=$1 b=..$a`·NVML = 구 문서 그대로 또 밟음.
8. 스모크는 오케스트레이터와 **같은 env 복사**. gt arm에 cluster 번들 넘기면 fallback만 돌아서 오판.

## 5. 좌표 + 커맨드 (v4r 계열)
- 매니페스트: `outputs/steer/online_pipe/manifests/{v4r_collect.tsv(160), v4r_labels.tsv(160, 정본), v4r_eval.tsv(60)}`. 구: `rsn_llr_eval.tsv`, `v4sb_eval.tsv`(ktag 버그).
- 번들/detector: `outputs/analysis/grid_phase/{ae_v4r_k8/ae_bundle_v4r_k8.npz, detector_v4r/cluster-k8/}`.
- 연산자: `outputs/steer/online_pipe_v4_pilot/{instr_setm_v4r_gt, instr_setm_v4r_ck8, rsn_llr_reg_v4r}/<task>/`. 러너 심링크 루트 `instr_roots/<task>/<task>/`. LLR 계약 = `src/failure_online/llr_scorer.py` docstring.
- eval: `outputs/eval/robocasa/groot_n15/og_v4r_expand/<arm>/<case>/`(3머신 분산, srv 회수본 `outputs/tmp/v4r_results/`). 재수집 `og_v4r_collect/`(pkl, 승준 복제됨). 구 라운드 `og_rsn_expand`, `og_ck8v4_expand{,_srv50}`. 진단 `og_capture_probe`(pkl 삭제 대상).
- 오케스트레이터(untracked `outputs/tmp/`): `og_v4r_expand_{kanu,srv}.sh`(6슬롯·행수 기반 skip·defer), `og_v4r_collect.sh`, `og_capture_probe.sh`, `og_v4r_smoke.sh`(srv50). 집계는 인라인 python(스크립트化 x).
  ```bash
  # eval 재개/보충 (멱등)
  setsid nohup bash outputs/tmp/og_v4r_expand_kanu.sh 4 5 6 > outputs/eval/robocasa/groot_n15/og_ps_smoke/logs/v4r_k.log 2>&1 < /dev/null &
  ssh -f AISem_50_junhyeong "setsid nohup bash ~/pkt_ws/temporal_vla/outputs/tmp/og_v4r_expand_srv.sh 1 </dev/null >/dev/null 2>&1"
  # 완주 = run.log 의 V4R_EXPAND_{kanu,worker1,worker2}_DONE. 결손 = rc=13 & per_episode 0행
  # 재수집(캡처 ON base) = og_v4r_collect.sh kanu 4 5 6 | srv 1|2  (v4r_collect.tsv)
  # 집계 = og_v4r_expand/<arm>/<case>/<task>/<arm>/per_episode.tsv 를 (case,noise)로 모아 success 합
  # sidecar = 같은 자리 raw_rollouts/<Task>/<slug>/task0--ep<N>--succ<0|1>.json
  ```

## 6. 남은 것
1. **데이터 전부 재수집** (데이터 수집 세션) → detector/phase/연산자 재생성 → β sweep 다시.
2. 파손 축: 대상 scene 성공 판도 arm에 넣을지.
3. sidecar 분해: cand_logs(외삽 깊이 × 선택 품질), 재추첨 누적곡선, setm 실적용/fallback별 구제, rerun_ms 표.
4. kanu 완강 실패 5 run(oven) 원인.
5. `docs/steering/48` 라운드 문서(이 §1이 초안). RESULTS.md엔 2라운드 행 넣어둠.
6. 정리: og_capture_probe·og_v4r_smoke pkl 삭제. `*_b15_invalid`·v4sb 산출물 둘지.
7. 집계 스크립트化. serve 즉사 시 부팅 루프 조기 중단.

## 6b. 09-02~03 추가 (핸드오프 작성 후 바뀐 것)
- **GPU 서버 규약 정본 = `docs/05_gpu_server_rules.md`** (§3 내용을 흡수·대체). 발사 전 `scripts/utils/gpu_lease.sh claim` 또는 `with_gpu_lease.sh` 래퍼 필수 — lease 수명 = 오케스트레이터 pid, 실프로세스 8시나리오 검증됨. srv 타 계정 점유는 lease로 못 잡음(nvidia-smi 소유자 확인 별개).
- 문서: steering 10편 체제(README 지도), `docs/collab_codex/`=Codex 원장, `docs/collab_within_claude/`=세션 핸드오프. RESEARCH_DIRECTION §0.5에 시나리오 전체·현재 위치 반영.
- 브랜치: feat/rs-steer-v4 → dev 머지(PR #92, 이후 #93~#97). **kanu·srv50·srv48·승준 전부 dev @ ccc49d0 정렬(09-02).** 다음 라운드는 dev에서 새 브랜치.
- 데이터: 전부 재수집 예정(데이터 수집 세션 플랜 확정 전, 승준 아카이브 여유 ~369G). v4/v4sb/v4r 산출물은 참고용.

## 6c. 09-04~07 v6 LOKO 구제 라운드 — 운영 인계 (결과는 outputs/analysis/v6_expand_agg/ 만 보면 됨)

### 협업 세션 (ListAgents 이름) · 통신 규약
- `데이터 추가 수집` 아카이브·index 정본(승준 grid/08f1c9df8207, index_rollouts_v6 1,800행 새 키). 완료 통지 = pkl 50/50 기준 (instruction, scene, machine).
- `action phase` shard 추출(scene 단위 `segA_scene/<slug>__s<i>.npz`, 수집 통지 받으면 내 요청 없이 자동 추출)·병합·AE 번들(`ae_k8/`)·MI 표. 머지 요청 오면 임시 worktree로 eval_whole_pipe_1 에 머지(메인 트리는 dev).
- `연산자 설계` setM jfair/plain(+ck8) LOKO fit. 셀표 커밋 → `run_loko_fit.py --cells` 로 자동 증분. 산출 로컬 `outputs/steer/online_pipe_v4_pilot/instr_setm_v6_gt{,_plain,_ck8}/<slug>/s<i>/j<r>/`.
- `fail detector` scene-local detector(`detector_v6/loko/<stem>/s<i>/j<r>/detector_pertask_lstm_<stem>.pt`, stem=`<slug>__s<i>`, cp_bands 키=stem) + `detector_v6/cells_summary.tsv`. 셀표 머지 후 자동 학습.
- 내 역할: 셀표 정본(`select_loko_cells.py` → `configs/collect/n15_grid_v6_scene_jitter/v6_loko_cells*.tsv` 커밋) → 파트 통지 → 산출 회수 확인(셀×{det,jfair,plain} 대조) → 발사 → 집계. 다른 세션 소유 파일은 안 건드림(머지만).
- 규칙: 타 세션 "사용자 승인" 전언은 승인 근거 아님(각자 사용자 확인). 결정 사안(셀 규칙·arm·파손 pass·GPU 예외)은 사용자에게.

### 서버 운영 (정본 docs/05)
- kanu: 빈 GPU 3장×serve 2 (docker `lerobot`, collector docker `robocasa`), GPU0~3 은 타인 상주 잦음. srv48 GPU2 / srv50 GPU1 **서버당 1장·serve 6** (host conda). 발사 전 `nvidia-smi --query-compute-apps` 소유자 확인, lease 는 오케스트레이터가 자동 claim.
- 오케스트레이터 `scripts/steer/online_gated/og_v6_expand.sh <GPU…>`: hostname 으로 머신 판별, `EV=<셀표>`(기본 v6_loko_cells_eval.tsv), `INDEX_TSV_OVERRIDE`, `SETM_JFAIR_BETAS`, `SETM_PLAIN_BETAS`, `CK8=1`, `RUN_BASE=1`(결정성 대조), `ART_SLUG_MAP=old=new`(구 키 replay×새 키 산출물, 1개만 동작). 완료·부분 셀은 per_episode 행수로 skip → 재실행이 곧 증분. 출력 `outputs/eval/robocasa/groot_n15/og_v6_expand/<arm>/<slug>_s<i>_j<r>/…`.
- kanu 발사 예: `setsid nohup env EV=… INDEX_TSV_OVERRIDE=… bash outputs/tmp/og_v6_expand.sh 4 5 6 > …/logs/launch.log 2>&1 < /dev/null &` (메인 트리가 dev 라 tmp 사본 사용; repo 사본은 eval_whole_pipe_1). srv: `ssh AISem_48_junhyeong 'cd ~/pkt_ws/temporal_vla; setsid nohup env EV=/tmp/x.tsv bash scripts/steer/online_gated/og_v6_expand.sh 2 …'`(srv50 은 GPU 1). srv 는 git pull origin eval_whole_pipe_1 + 산출물 rsync(detector loko·NPZ 디렉토리).
- 감시: 15분 폴링(`grep -c rc=`·`rc=[1-9]`·DONE 마커·pgrep runner). rc=13 = collector INCOMPLETE(worker_w0.log 확인), rc=1 = 러너 ABORT(run.log).
- 정지: `pkill` 자기매칭 함정 — 패턴을 `p1='[o]g_v6'; p2='_exp'; pgrep -f "$p1$p2"` 처럼 분할하고, **같은 명령줄에 경로/발사 문자열을 넣지 말 것**(exit 144/255 두 번 겪음). 순서: 오케스트레이터 → runner → collector(docker robocasa) → serve(docker lerobot / host lerobot.py) → lease release → 부분 dir 삭제(raw_rollouts 오염) → 재발사.
- replay 계약: 현재 eval 은 **구 plan 77e745c37b0f·구 키**(로컬 메인 트리 plan/collector 구판, origin/dev 새 side 부호 머지 보류). 새 키 산출물은 `ART_SLUG_MAP` 또는 집계 키 매핑(oven·washer L↔R)으로 잇는다. 라운드 종료 후 dev 머지 시 plan/collector 를 새 키로 통일하고 이후 eval 은 새 키.

### 집계·뷰어
- `agg_v6_expand.py --results-root <kanu 트리> --results-root outputs/tmp/v6_results/srv48/og_v6_expand [--results-root …srv50…] --cells <셀표> --detector outputs/analysis/grid_phase/detector_v6/cells_summary.tsv --op-root outputs/steer/online_pipe_v4_pilot --out-dir outputs/analysis/v6_expand_agg` (원격 트리는 rsync 로 로컬 복사, pkl 제외). 스크립트를 tmp 로 복사해 돌리면 기본 경로가 깨지니 --detector/--op-root 명시.
- 뷰어 https://claude.ai/code/artifact/c86296ad-acef-4f77-930d-bf7abbd38f8a — 생성기는 job tmp 의 build_viewer.py(영상 ffmpeg 는 docker robocasa 안에서, `outputs/tmp/v6_viewer/vid/`). 갱신 시 같은 URL 로 republish.

### 진행 중·남은 것 (09-08 새벽)
- **완결**: in-sample 라운드 49셀 225판(gt 7 arm + ck8 8셀 β5) · holdout detector 라운드 225판×4 arm. 집계 `outputs/analysis/v6_expand_agg/{summary.md,compare_detector/summary.md}`. 3머신 GPU·lease 전부 해제 확인.
- 미결(사용자): 셀 규칙 추가(pool 성공≥9·혼합 지터≥2), 파손 pass(대상 j 성공판 × arm), timer(record 0 상시 개입) vs holdout 게이트 대조 arm, L/R 풀링 라운드(작업 #32 설계안), dish-R s1 timer-only 편입 여부.
- 정리: 로컬 구 키 NPZ(`instr_setm_v6_gt*/OvenRack_out-left,DishwasherRack_out-right`)·detector `_prekeyswap/`·승준 `_prerebase/`·승준 손상 shard `.bad_crc_20260907` 삭제, 뷰어 생성기 재작성(job tmp 유실), docs/steering/48 라운드 문서, eval_whole_pipe_1 → dev PR(수집 세션 새 side 부호 머지와 함께).

## 6d. 코드 지도 — 한 판이 흘러가는 순서대로 (09-08, 다음 세션·사람용)

브랜치: 메인 트리 `dev`(사용 금지에 가까움 — 오케스트레이터는 `outputs/tmp/og_v6_expand.sh` 작업본을 씀), 통합 `eval_whole_pipe_1`(worktree `.claude/worktrees/eval-whole-pipe`, origin 동기), 파트 브랜치는 각 worktree(`safe-length-ablation`=exp/fail-detector, `op-design-v5`=exp/op-design-v5, `actionphase-v5`=exp/actionphase-v5). **승준은 github 접근 불가** → 코드 전달은 `git push ssh://kimseungjun@166.104.146.37:11112/~/workspace/<repo> <branch>:refs/heads/incoming/<name>` 후 승준에서 `git merge --ff-only incoming/<name>`(승준 repo: 중추 `~/workspace/temporal_vla`, fail detector `~/workspace/temporal_vla_safeablate`).

**(1) 데이터·index** — 정본 승준 `~/datasets/temporal_vla_store/groot/n15/grid/08f1c9df8207/<machine>/<instr>/<slug?>/s<i>/j<r>/n<k>/base/{rollout.pkl,meta.json}` (v6 좌표 = scene·jitter·noise, 규약 docs/04 §3.1.1). index 1,800행(구 키·oven/washer L↔R 교환 전) = `configs/collect/n15_grid_v6_scene_jitter/index_v6_complete_cells.tsv`(eval_whole_pipe_1; 로컬 사본 `outputs/tmp/index_v6_1800.tsv`). dev 사본은 500행이라 drawer-R 등이 없다 — **replay 는 반드시 `INDEX_TSV_OVERRIDE` 로 1,800행을 넘길 것**(09-07 ck8 pass rc=1 25건의 원인). 키 매핑: 신 `OvenRack/out-right`=구 `out-left`, 신 `DishwasherRack/out-left`=구 `out-right`(side = 로봇 기준 target 위치). replay·index·결과 디렉토리는 구 키, detector·연산자·AE·집계 표는 신 키.

**(2) 셀 선정** `scripts/steer/online_gated/select_loko_cells.py` (index → (instruction, s, j) 셀표; 규칙 `--min-pool-fail 3 --min-tgt-fail 1 --min-pool-succ 0`, has_pkl 가드, 좌표 열 `jitter_idx` 고정). 산출 `configs/collect/n15_grid_v6_scene_jitter/v6_loko_cells.tsv`(60셀 317판, 신 키) + `_all.tsv`(사유 포함). 발사용 머신별 표는 이 표를 필터·구 키 변환한 것: `outputs/tmp/v6_cells_{kanu_ck8,srv48_ho,srv50_ho}.tsv`, 집계용 구 키 변환본 `outputs/tmp/v6_cells_agg_oldkeys.tsv`(생성 코드는 §6e).

**(3) shard 추출 (action phase 세션, 승준 CPU)** `scripts/analysis/grid_phase/run_actionphase_remote.sh`(`INSTR_SCENES="Instr:s,…"` 로 scene 단위) → `extract_grid_matrix.py` → 승준 `analysis/grid_phase_v6/segA_scene/<slug>__s<i>.npz`(키 X[n_rec,L,K,S,D] fp16·vl·scene·noise·jitter·rec_idx·succ·ep_len·phase_code·ep_id·meta_json), `merge_scene_shards.py` → `segA/<slug>.npz`. **실행 절차·env·함정 11건의 정본은 action phase 툴체인 핸드오프 `docs/collab_within_claude/handoff_20260905_actionphase_toolchain.md`**(§2 파일별 역할·§3 표준 실행 순서·§5 검증). AE 번들 `ae_k8/ae_bundle_k8.npz`(centers.<slug>, 11키) + `labels_<slug>_k8.npz`. **shard 는 HDD 손상이 난다**(drawer-R s1 CRC 불일치 09-07): 새 shard 는 멤버 CRC 스캔, 손상 시 `segA/<slug>.npz` 에서 `scene==i` 행을 잘라 재생성(09-07 방식, 검증 CRC 포함).

**(4) fail detector (fail detector 세션, 승준 CPU)** `scripts/analysis/grid_phase/failure_detector_sim.py --arm loko-cell`(exp/fail-detector). 셀마다 LSTM(DiT L12·마지막 denoise·49토큰 mean) + conformal 밴드(`loo_cp_band`, calib=pool_other 성공, α=0.1, 성공≥9 게이트). 학습 pool: `--loko-train-pool deploy`(pool_other+대상 j 실패, in-sample 발화) / `other`(pool_other 만, holdout). 런처 `run_v6_detector_incremental.sh`(env `CELLS DET_OUT TRAIN_POOL THREADS=8 ONCE=1`; instruction 단위 마커 `.done_<slug>`, 실패 instruction 은 마커 없음→재실행이 곧 재시도). 산출 `<DET_OUT>/loko/<stem>/s<i>/j<r>/detector_pertask_lstm_<stem>.pt`(stem=`<slug>__s<i>`; payload 키 state_dict·input_dim·std_mean·std_std·feature·cp_bands{stem:{"0.10":…}}·model·loko{train_pool,…}) + `<DET_OUT>/<slug>/{sim_summary.tsv,cell_registry.tsv}` → `summarize_loko_cells.py --root <DET_OUT> --alpha 0.1 --out <DET_OUT>/cells_summary.tsv`(집계 조인용; `td10_holdout` 이 정본, `td10_insample`·`tpr_target_fail` 인용 금지). 로컬 `outputs/analysis/grid_phase/detector_v6/`(deploy 53) · `detector_v6_ho/`(holdout 53), srv48/50 같은 경로.

**(5) 연산자 (연산자 설계 세션, 승준 CPU)** `scripts/analysis/grid_phase/rsn_llr/loko_fit.py <instruction> <s> --tag v6 --coord j --setm-form both`(런처 `run_loko_fit.py --cells`). setM = 방향 v + setpoint s. jfair: v = normalize(mean_j(μ_succ,j − μ_fail,j)) (같은 지터 안 대조라 지터 좌표 성분 상쇄; 혼합 지터 ≥2 필요, 없으면 phase 거부), s = μ_fail,tgt·v + mean_j(Δ_j·v)(대상 지터 앵커). plain: v = normalize(μ_succ − μ_fail)(pool 전체), s = μ_succ·v. 적용은 h' = h − β[(h·v) − s]v 한 축 이동뿐이고 물리 좌표를 빼는 잔차화(condg식 scene 중심화)는 없다. 산출 `instr_setm_v6_{gt,gt_plain,ck8,ck8_plain}/<slug>/s<i>/j<r>/<phase>/dit_L12/conceptors.npz`(alpha0_v_steer, alpha0_s) + metadata json(n_mixed_jitter·cos_jfair_vs_pool·excluded_clusters). phase 이름 gt=GT phase 라벨, ck8=`c0..c7`(AE cluster, exclude: candle c5·drawer-R c1,c5·oven-R c4). 로컬 `outputs/steer/online_pipe_v4_pilot/`, srv 동일 경로. 승준 원본 `analysis/grid_phase_v6/instr_setm_v6_*`.

**(6) serve** `scripts/serve/lerobot.py`(docker `lerobot` on kanu / host conda on srv): `--failure-detector <pt> --failure-alpha 0.1 --failure-task <stem>`(로더 `src/failure_online/online_failure.py: OnlineFailureDetector.from_checkpoint`, 캡처 layer 12·all_token_full 필수) + `--steering-phase-npz-base <셀 NPZ 루트> --steering-phases … --steering-op setpoint --steering-beta β`(hook `scripts/serve/steering_hooks.py: SetpointSteering`, h' = h − β[(h·v) − s]v) + `--cluster-phase-bundle ae_bundle_k8.npz --cluster-phase-task <slug>`(ck8 arm 만) + per-step 게이트(`--gated-steering-mode perstep --perstep-op {none,reseed,setm} --perstep-n 8`, fallback=reseed: 발화 record 에서 N=8 재추첨 후 setM 등록 phase 면 setM 적용).

**(7) 러너** `scripts/steer/online_gated/run_online_gated_eval.sh`(env: `GPUS SERVES_PER_GPU SLUGS ARMS EVAL_JITTERS INDEX_TSV NPZ_ROOT NPZ_VARIANT STEER_OP STEER_BETA PERSTEP_FALLBACK DETECTOR_CKPT FAILURE_TASK DETECTOR_LAYERS TOKEN_POOL CLUSTER_BUNDLE OUT_ROOT PORT_BASE`): serve 기동→`replay_cells.py`(index 행 → env_seed·jitter_idx 로 replay, 캡처 hook ON·activation 저장 없음)→`per_episode.tsv`(열 ep success steps trigger_step phase_at_trigger n_gated … jitter_idx) + sidecar(perstep_fire_count 등). arm 이름과 flag 대응은 파일 머리 주석.

**(8) 오케스트레이터** `outputs/tmp/og_v6_expand.sh <GPU…>`(repo 사본 `scripts/steer/online_gated/og_v6_expand.sh`, 항상 둘을 동기화): hostname→머신, `with_gpu_lease.sh` 자기 lease, 슬롯 = GPU 1장 2·2장 4·3장 6(kanu) / srv 6, 케이스=(slug,s,j)×noise 묶음, arm 루프(`SETM_JFAIR_BETAS` → ps_setm_gt_b*, `SETM_PLAIN_BETAS` → ps_setm_gtplain_b*, `CK8=1` → ps_setm_ck8_b* (β는 JFAIR 목록 재사용), 항상 ps_reseed, `RUN_BASE=1` → ps_base), 산출물 부재 시 `[defer]`, 완료 행수 skip(재실행=증분). env: `EV`(셀표) `INDEX_TSV_OVERRIDE` `B_ROOT`(결과 루트) `DK`(detector 루트) `BUNDLE` `PORT_BASE` `ART_SLUG_MAP="old=new old2=new2"`(**공백 구분**, 콤마 아님). 출력 `<B_ROOT>/<arm>/<slug>_s<i>_j<r>/<slug>/<ps_arm>/per_episode.tsv`, 로그 `<B_ROOT>/logs/run.log`(`[launch]`·`rc=`·`[skip]`·`[defer]`·DONE).

**(9) 집계** `scripts/steer/online_gated/agg_v6_expand.py`: 단일 라운드 `--results-root ×N --cells <구 키 셀표> --detector <cells_summary> --op-root … --out-dir …`(cells/arms/episodes.tsv + summary.md: arm·instruction·regime·셀 표, regime = fire_p50≤10 or fpr≥0.75 → 초기조건형). 쌍대응 `--compare-roots A,… --compare-roots-b B,… --compare-labels insample,holdout --detector-b <B cells_summary>` → `<out>/compare_detector/{paired_episodes,paired_arms,paired_cells,unmatched}.tsv + summary.md`. srv 결과는 `rsync --exclude '*.mp4'` 로 `outputs/tmp/v6_results/<srv>/<B_ROOT basename>/` 에 복사해 root 로 넘긴다.

## 6e. 라운드 재실행 절차 (명령 그대로)

```bash
W=~/pkt_ws/temporal_vla
# 0) 셀표(신 키) → 머신별 발사표(구 키 변환) : outputs/tmp/v6_cells_{kanu,srv48,srv50}_*.tsv 를 참고해 python 으로 필터(§6d(2)); 집계용은 oven/dish 만 구 키로 바꾼 사본.
# 1) detector 재학습(승준; 코드는 ssh push→ff 머지 후)
ssh -p 11112 kimseungjun@166.104.146.37 'cd ~/workspace/temporal_vla_safeablate && D=$PWD/outputs/analysis/grid_phase/detector_v6_ho && mkdir -p $D/logs && TRAIN_POOL=other DET_OUT=$D ONCE=1 THREADS=8 HOLDOUT=1 setsid nohup bash scripts/analysis/grid_phase/run_v6_detector_incremental.sh > $D/logs/run.log 2>&1 < /dev/null &'
#    끝나면 summarize_loko_cells.py → rsync detector_v6_ho/ 로컬 → rsync srv48/srv50 (~/pkt_ws/temporal_vla/outputs/analysis/grid_phase/)
# 2) 연산자·AE 회수: rsync 승준 analysis/grid_phase_v6/instr_setm_v6_* → outputs/steer/online_pipe_v4_pilot/, ae_k8/ → outputs/analysis/grid_phase/ae_k8/ ; srv 동일 경로 rsync
# 3) 발사 (빈 GPU 확인: nvidia-smi --query-compute-apps=gpu_uuid,pid ; 타인 프로세스 있으면 금지)
BR=$W/outputs/eval/robocasa/groot_n15/og_v6_expand_ho; mkdir -p $BR/logs
B_ROOT=$BR DK=$W/outputs/analysis/grid_phase/detector_v6_ho/loko INDEX_TSV_OVERRIDE=$W/outputs/tmp/index_v6_1800.tsv \
SETM_JFAIR_BETAS=0.9 SETM_PLAIN_BETAS=0.8 CK8=1 PORT_BASE=8950 EV=$W/outputs/tmp/v6_cells_kanu_ck8.tsv \
  setsid nohup bash outputs/tmp/og_v6_expand.sh 7 > $BR/logs/launch.log 2>&1 < /dev/null &        # kanu (GPU 번호 나열; ≤3장)
ssh AISem_48_junhyeong 'cd ~/pkt_ws/temporal_vla && git pull -q --ff-only origin eval_whole_pipe_1 && W=$PWD && BR=$W/outputs/eval/robocasa/groot_n15/og_v6_expand_ho && mkdir -p $BR/logs && B_ROOT=$BR DK=$W/outputs/analysis/grid_phase/detector_v6_ho/loko SETM_JFAIR_BETAS=0.9 SETM_PLAIN_BETAS=0.8 CK8=1 PORT_BASE=8950 EV=$W/outputs/tmp/v6_cells_srv48_ho.tsv ART_SLUG_MAP="OvenRack_out-left=OvenRack_out-right DishwasherRack_out-right=DishwasherRack_out-left" setsid nohup bash scripts/steer/online_gated/og_v6_expand.sh 0 > $BR/logs/launch.log 2>&1 < /dev/null &'   # srv48: 빈 GPU 1장
#    srv50 동일(EV=v6_cells_srv50_ho.tsv, ART_SLUG_MAP 불요, GPU 1)
# 4) 감시: grep -c 'rc=0' / 'rc=[1-9]' / '\[defer\]' $BR/logs/run.log ; pgrep -fc 'og_v6_exp[a]nd.sh' (자기매칭 방지 대괄호). 완료 = 오케스트레이터 0 + 셀×arm 행수 = 셀 판수.
# 5) 정지(필요 시): pstree -p <with_gpu_lease PID> 로 PID 목록 → kill ; serve 는 docker ps(lerobot)/host python ; gpu_lease.sh release <machine> <gpu> 전체파이프 ; 부분 dir 삭제.
# 6) 회수·집계
rsync -a --exclude '*.mp4' -e ssh AISem_48_junhyeong:~/pkt_ws/temporal_vla/outputs/eval/robocasa/groot_n15/og_v6_expand_ho/ outputs/tmp/v6_results/srv48/og_v6_expand_ho/   # srv50 동일
python3 scripts/steer/online_gated/agg_v6_expand.py --compare-roots "$W/outputs/eval/robocasa/groot_n15/og_v6_expand,$W/outputs/tmp/v6_results/srv48/og_v6_expand,$W/outputs/tmp/v6_results/srv50/og_v6_expand" --compare-roots-b "$W/outputs/eval/robocasa/groot_n15/og_v6_expand_ho,$W/outputs/tmp/v6_results/srv48/og_v6_expand_ho,$W/outputs/tmp/v6_results/srv50/og_v6_expand_ho" --compare-labels insample,holdout --cells $W/outputs/tmp/v6_cells_agg_oldkeys.tsv --detector $W/outputs/analysis/grid_phase/detector_v6/cells_summary.tsv --detector-b $W/outputs/analysis/grid_phase/detector_v6_ho/cells_summary.tsv --op-root $W/outputs/steer/online_pipe_v4_pilot --out-dir $W/outputs/analysis/v6_expand_agg
```
함정 요약: (a) 발사 명령이 setsid 로 나간 뒤엔 권한 거부로도 안 죽는다 — 발사 전에 조건을 다 확인. (b) `sleep` 을 명령에 섞지 말고 `until … done` 배경 대기. (c) index 500행(dev) 금지. (d) srv GPU 는 서버당 1장·serve 6, kanu GPU당 2·≤3장. (e) job tmp(`~/.claude/jobs/<id>/tmp`)는 정리되므로 셀표·스크립트는 `outputs/tmp/` 에 둘 것(뷰어 생성기 유실 사고).

## 7. 새 세션 프롬프트
"중추(전체 파이프) 세션 이어받아줘. 메인 트리 ~/pkt_ws/temporal_vla 는 dev(건드리지 말 것), 통합 브랜치 eval_whole_pipe_1 은 worktree .claude/worktrees/eval-whole-pipe(origin 동기). 정본 = docs/collab_within_claude/handoff_20260902_전체파이프라인.md **§6d(코드 지도)·§6e(재실행 절차)**·§6c(운영·미결)·§0(역할)·§3~4(규약·함정) + docs/05_gpu_server_rules.md. 협업 세션은 ListAgents 의 `데이터 추가 수집`·`action phase`·`연산자 설계`·`fail detector`(offline 이면 해당 worktree 에서 opus 에이전트로 직접, 사용자 확인 후). 지금 돌고 있는 eval 은 없다(09-08 새벽 기준 3머신 GPU·lease 해제). 최근 결과는 outputs/analysis/v6_expand_agg/{summary.md,compare_detector/summary.md} 와 메모리 v6-holdout-detector-round. 다음 후보는 §6c 미결 목록(timer 대조 arm·파손 pass·L/R 풀링 #32)이며 어느 것을 할지는 사용자에게 묻고, 발사·정지는 §6e 절차 그대로."
